lm:
  cors:
    admin-origins:
      - http://localhost:4200

spring:
  jpa:
    show-sql: true
  liquibase:
    contexts: app,seed

logging:
  structured:
    format:
      # Empty switches structured logging off — Boot applies the JSON encoder only when
      # this property has text.
      console: ""
